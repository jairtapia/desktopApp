<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="DesktopAssistant.Views.LoginPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="using:DesktopAssistant.Converters">

    <Page.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibility" />
    </Page.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <!-- Left Panel - Branding -->
        <Grid Grid.Column="0" Background="{ThemeResource SystemAccentColorDark2}">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Spacing="16" Padding="60">
                <FontIcon Glyph="&#xE770;" FontSize="72" Foreground="White" HorizontalAlignment="Center" />
                <TextBlock Text="Desktop Assistant" 
                           FontSize="36" FontWeight="Bold" 
                           Foreground="White" HorizontalAlignment="Center" />
                <TextBlock Text="Control your desktop remotely.&#x0a;Connect, scan, and execute actions&#x0a;from anywhere."
                           FontSize="16" Foreground="#CCFFFFFF" 
                           HorizontalAlignment="Center" TextAlignment="Center"
                           LineHeight="24" />

                <Border Background="#33FFFFFF" CornerRadius="12" Padding="20" Margin="0,24,0,0" MaxWidth="360">
                    <StackPanel Spacing="12">
                        <StackPanel Orientation="Horizontal" Spacing="12">
                            <FontIcon Glyph="&#xE73E;" FontSize="16" Foreground="#99FFFFFF" />
                            <TextBlock Text="Scan installed applications" Foreground="#CCFFFFFF" FontSize="14" />
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Spacing="12">
                            <FontIcon Glyph="&#xE73E;" FontSize="16" Foreground="#99FFFFFF" />
                            <TextBlock Text="Remote volume &amp; brightness control" Foreground="#CCFFFFFF" FontSize="14" />
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Spacing="12">
                            <FontIcon Glyph="&#xE73E;" FontSize="16" Foreground="#99FFFFFF" />
                            <TextBlock Text="Split screen management" Foreground="#CCFFFFFF" FontSize="14" />
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Spacing="12">
                            <FontIcon Glyph="&#xE73E;" FontSize="16" Foreground="#99FFFFFF" />
                            <TextBlock Text="Pair with your mobile device" Foreground="#CCFFFFFF" FontSize="14" />
                        </StackPanel>
                    </StackPanel>
                </Border>
            </StackPanel>
        </Grid>

        <!-- Right Panel - Login Form -->
        <Grid Grid.Column="1" Width="440" Padding="48">
            <StackPanel VerticalAlignment="Center" Spacing="8" MaxWidth="340">
                
                <TextBlock Text="Welcome back" FontSize="28" FontWeight="SemiBold" Margin="0,0,0,4" />
                <TextBlock Text="Sign in to your account" 
                           FontSize="14" Foreground="{ThemeResource TextFillColorSecondaryBrush}" 
                           Margin="0,0,0,24" />

                <!-- Error Message -->
                <InfoBar x:Name="ErrorInfoBar"
                         IsOpen="{x:Bind ViewModel.HasError, Mode=OneWay}"
                         Severity="Error"
                         Title="Login Failed"
                         Message="{x:Bind ViewModel.ErrorMessage, Mode=OneWay}"
                         IsClosable="True"
                         Margin="0,0,0,12" />

                <!-- Email -->
                <TextBlock Text="Email" FontSize="13" FontWeight="SemiBold" Margin="0,0,0,4" />
                <TextBox x:Name="EmailBox"
                         Text="{x:Bind ViewModel.Email, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                         PlaceholderText="you@example.com"
                         Padding="12,10"
                         CornerRadius="8" />

                <!-- Password -->
                <TextBlock Text="Password" FontSize="13" FontWeight="SemiBold" Margin="0,12,0,4" />
                <PasswordBox x:Name="PasswordBox"
                             Password="{x:Bind ViewModel.Password, Mode=TwoWay}"
                             PlaceholderText="Enter your password"
                             Padding="12,10"
                             CornerRadius="8" />

                <!-- Login Button -->
                <Button x:Name="LoginButton"
                        Content="Sign In"
                        Style="{StaticResource AccentButtonStyle}"
                        HorizontalAlignment="Stretch"
                        Padding="0,12"
                        CornerRadius="8"
                        Margin="0,20,0,0"
                        Command="{x:Bind ViewModel.LoginCommand}" />

                <!-- Loading indicator -->
                <ProgressBar IsIndeterminate="True"
                             Visibility="{x:Bind ViewModel.IsLoading, Mode=OneWay, Converter={StaticResource BoolToVisibility}}"
                             Margin="0,8,0,0" />

                <!-- Separator -->
                <Grid Margin="0,24,0,24">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Border Grid.Column="0" Height="1" Background="{ThemeResource DividerStrokeColorDefaultBrush}" VerticalAlignment="Center" />
                    <TextBlock Grid.Column="1" Text="or" FontSize="13" Foreground="{ThemeResource TextFillColorSecondaryBrush}" Margin="12,0" />
                    <Border Grid.Column="2" Height="1" Background="{ThemeResource DividerStrokeColorDefaultBrush}" VerticalAlignment="Center" />
                </Grid>

                <!-- Register Link -->
                <Button Content="Create an account"
                        HorizontalAlignment="Stretch"
                        Padding="0,12"
                        CornerRadius="8"
                        Command="{x:Bind ViewModel.GoToRegisterCommand}" />
            </StackPanel>
        </Grid>
    </Grid>
</Page>
